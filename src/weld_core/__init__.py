"""Weld point candidate detection core (CATIA-independent).

This package holds the pure-Python algorithm layer. It must never import
pycatia or pywin32 so that it stays fully testable on any platform.
"""

__version__ = "0.1.0"
